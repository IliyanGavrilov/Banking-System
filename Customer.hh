#pragma once


class Customer {
public:
  // float check_avl(bank_name, account_number)
  // bool/void open(bank_name)
  // close(bank_name, account_number)
  // redeem(bank_name, account_numer, verification_code)
  // change(new_bank_name, current_bank_name, account_number)
  // list(bank_name)
  // messages()
};
